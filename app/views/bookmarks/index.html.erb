<h2>Twoje zakładki</h2>

<table style="width: 100%">
  <thead>
    <tr>
      <th>Title</th>
      <th>Url</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @bookmarks.each do |bookmark| %>
      <tr>
        <td><%= bookmark.title %></td>
        <td><%= bookmark.url %></td>
        <td><%= link_to 'Pokaż', bookmark %></td>
        <td><%= link_to 'Edytuj', edit_bookmark_path(bookmark) %></td>
        <td><%= link_to 'Usuń', bookmark, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @bookmarks %>

<%= link_to 'Nowa zakładka', new_bookmark_path, class: 'button' %>
